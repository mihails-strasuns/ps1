# Simple git-friendly PS1 script

- Shows the current branch and commit when inside a git repo
- Two line output to ensure fixed cursor location
- Exit status for the last command

![example](example.png)
